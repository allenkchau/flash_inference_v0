An inference engine built from scratch. Can run a transformer model (also built from scratch) with
different KV cache optimizations to compare latency and throughput.

AI Usage:

Writing unit tests. Everything else was written by myself.
